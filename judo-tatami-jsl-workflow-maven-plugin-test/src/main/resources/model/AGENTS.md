# AGENTS.md — `judo-tatami-jsl-workflow-maven-plugin-test/src/main/resources/model`

Sample JSL model used by workflow Maven plugin integration tests.

| File | Purpose |
| --- | --- |
| `Test.jsl` | Defines `Test` JSL model with `Person` entity, `User` entity, `UserActor` actor with realm/claim/identity, and `UserTransfer` mapping for plugin test executions. |
