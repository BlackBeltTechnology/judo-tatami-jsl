# AGENTS.md — `judo-tatami-jsl-jsl2ui/src/main/epsilon/operations/jsl`

EOL operation bundle aggregating domain-specific operations across actor, data, namespace, and UI scopes.

| File | Purpose |
| --- | --- |
| `_importAll.eol` | Root aggregator script importing all JSL EOL helper operations across subdirectories. Imports `actor/_importActor.eol`, `data/_importData.eol`, `namespace/_importNamespace.eol`, and `ui/_importUI.eol`. |
