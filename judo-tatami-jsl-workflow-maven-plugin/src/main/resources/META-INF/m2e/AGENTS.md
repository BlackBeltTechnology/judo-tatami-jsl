# AGENTS.md — `judo-tatami-jsl-workflow-maven-plugin/src/main/resources/META-INF/m2e`

M2Eclipse lifecycle mapping configuration for the JSL workflow Maven plugin.

| File | Purpose |
| --- | --- |
| `lifecycle-mapping-metadata.xml` | Configures Eclipse m2e execution metadata for `default-model-workflow` goal. Specifies `<execute>` action with `runOnIncremental=false` and `runOnConfiguration=true`. |
