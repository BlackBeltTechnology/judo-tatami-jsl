@cached
operation JSL!ErrorField getId(): String {
    return (self.eContainer.getId() + "/" + self.name);
}


@cached
operation JSL!ErrorField getFqName(): String {
    return self.eContainer.getFqName() + "#" + self.name;
}
