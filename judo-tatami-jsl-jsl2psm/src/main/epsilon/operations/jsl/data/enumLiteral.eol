@cached
operation JSL!EnumLiteral getId(): String {
    return (self.eContainer.getId() + "/" + self.name);
}

@cached
operation JSL!EnumLiteral getFqName(): String {
    return self.eContainer.getFqName() + "#" + self.name;
}
