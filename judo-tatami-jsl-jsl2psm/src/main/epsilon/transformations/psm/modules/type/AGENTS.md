# AGENTS.md — `judo-tatami-jsl-jsl2psm/src/main/epsilon/transformations/psm/modules/type`

Epsilon Transformation Language (ETL) rules transforming JSL data types and enumerations into PSM primitive and enumeration types.

| File | Purpose |
| --- | --- |
| `type.etl` | Transforms `JSL!DataTypeDeclaration` (numeric, date, time, timestamp, string, binary, boolean) and `JSL!EnumDeclaration` into corresponding `JUDOPSM` primitive and enumeration types. |
