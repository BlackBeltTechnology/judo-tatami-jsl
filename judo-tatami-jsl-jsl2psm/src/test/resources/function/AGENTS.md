# AGENTS.md — `judo-tatami-jsl-jsl2psm/src/test/resources/function`

JSL test model definitions for instance functions and container navigation across imported models.

| File | Purpose |
| --- | --- |
| `ImportedTestInstanceFunctionModel.jsl` | Test model declaring entity `I` containing a collection of `TestInstanceFunctionModel::C` instances. |
| `TestInstanceFunctionModel.jsl` | Test model declaring entities `A`, `B`, and `C` with `self.container(...)` navigation using simple, fully qualified, and imported type references. |
