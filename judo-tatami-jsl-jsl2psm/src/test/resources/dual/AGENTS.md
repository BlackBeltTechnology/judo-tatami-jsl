# AGENTS.md — `judo-tatami-jsl-jsl2psm/src/test/resources/dual`

Dual-engine comparison test resources for cardinality and identity mapping.

| File | Purpose |
| --- | --- |
| `CardinalityIdTestModel.jsl` | Defines `Parent` and `Child` entities with 1-to-many relationship and matching transfer object mappings (`ParentT`, `ChildT`) for dual-engine parity testing. |
