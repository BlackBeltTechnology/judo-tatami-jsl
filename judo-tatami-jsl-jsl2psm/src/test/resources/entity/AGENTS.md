# AGENTS.md — `judo-tatami-jsl-jsl2psm/src/test/resources/entity`

JSL test model definitions for entity associations, opposites, inheritance, and field default expressions.

| File | Purpose |
| --- | --- |
| `AssociationRelationTestModel.jsl` | Test model specifying bidirectional association relations, opposite references (`opposite:` and `opposite-add:`), and multi-level inheritance hierarchies (`LeadChild`, `CustomerGrandChild`). |
| `TestDefaultExpressionModel.jsl` | Test model declaring literal default expressions across boolean, numeric, escaped/raw string, date, timestamp, and time fields in entities, identifiers, and errors. |
